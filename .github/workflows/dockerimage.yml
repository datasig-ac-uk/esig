name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      run: |
        cd build/Linux && pwd && ls -la && docker build -t esig_builder_linux_i686 -f Dockerfile_i686.dockerfile .
